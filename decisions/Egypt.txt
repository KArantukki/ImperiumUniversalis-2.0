country_decisions = {
	
	 form_egypt_nation = {
		major = yes
		potential = {
			OR = {
			TAG = MEM
			TAG = THB
			}
		}
		allow = {
			NOT = { exists = EGY }
			is_at_war =  no
			
			lower_egypt_region = { type = all owned_by = ROOT }
			upper_egypt_region = { type = all owned_by = ROOT }
		}
		effect = {
			change_tag = EGY
			}
		ai_importance = 500	
		ai_will_do = {
			factor = 400
			}
		}
}
